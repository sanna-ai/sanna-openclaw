{
  "id": "sanna",
  "name": "Sanna Governance",
  "version": "1.0.1",
  "description": "Constitution enforcement and cryptographic receipts for OpenClaw agent tool execution",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "constitutionPath": {
        "type": "string",
        "description": "Path to YAML constitution file or directory"
      },
      "privateKeyPath": {
        "type": "string",
        "description": "Path to Ed25519 private key PEM for receipt signing"
      },
      "publicKeyPath": {
        "type": "string",
        "description": "Path to Ed25519 public key PEM for receipt and constitution verification"
      },
      "receiptStorePath": {
        "type": "string",
        "description": "Path to SQLite receipt store database"
      },
      "enforcementMode": {
        "type": "string",
        "enum": ["enforce", "audit", "passthrough"],
        "default": "enforce"
      },
      "otelExport": {
        "type": "boolean",
        "default": false,
        "description": "Enable OpenTelemetry span export for governance receipts"
      },
      "otelServiceName": {
        "type": "string",
        "default": "sanna-openclaw",
        "description": "OpenTelemetry service name for exported spans"
      },
      "llmChecks": {
        "type": "boolean",
        "default": false,
        "description": "Enable LLM semantic checks for invariant evaluation"
      },
      "llmChecksModel": {
        "type": "string",
        "description": "Model to use for LLM semantic checks"
      },
      "customEvaluatorsPath": {
        "type": "string",
        "description": "Path to JS module that registers custom invariant evaluators"
      },
      "sinkType": {
        "type": "string",
        "enum": ["local_sqlite", "null"],
        "default": "local_sqlite",
        "description": "Receipt persistence sink type"
      },
      "contentMode": {
        "type": "string",
        "enum": ["full", "redacted", "hashes_only"],
        "default": "full",
        "description": "Content mode for receipt data: full content, redacted, or hash-only"
      }
    }
  },
  "uiHints": {
    "constitutionPath": { "label": "Constitution Path", "placeholder": "./constitutions" },
    "privateKeyPath": { "label": "Signing Key Path", "sensitive": true },
    "enforcementMode": { "label": "Enforcement Mode" },
    "llmChecksModel": { "label": "LLM Check Model", "placeholder": "claude-sonnet-4-5-20250929" }
  }
}
